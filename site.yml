- hosts: raspi
  user: ansible
  become: true
  become_method: sudo
  roles:
    - {role: 'initial', tags: 'initial'}
  vars:
    dist_upgrade: yes
    update_apt_cache: yes
    wireguard: no
    monitoring: no